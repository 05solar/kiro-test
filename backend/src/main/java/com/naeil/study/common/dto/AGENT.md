# common/dto — 에이전트 작업 규칙

## 지킬 것

- `record` 로 만든다.
- 정적 팩터리(`of`, `from`)로 생성한다.
- 사용자에게 보여줄 메시지만 담는다.

## 하지 말 것

- 성공 응답 공통 래퍼 도입
- 스택트레이스, 예외 클래스명, 내부 ID를 필드로 추가
- 도메인별 DTO를 여기에 두기 (도메인 패키지의 `dto` 에 둔다)
