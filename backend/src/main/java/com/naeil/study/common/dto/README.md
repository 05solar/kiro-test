# common/dto

모든 도메인이 공유하는 응답 형식.

| 클래스 | 역할 |
| --- | --- |
| `ErrorResponse` | 모든 에러 응답의 공통 형식 |

```json
{
  "code": "SESSION_NOT_FOUND",
  "message": "유효한 학습 세션을 찾을 수 없습니다."
}
```

`code` 는 클라이언트가 분기에 쓰고, `message` 는 사용자에게 그대로 보여줄 수 있는 문장이다.
따라서 메시지에 내부 정보(ID, 시각, 예외 클래스명)를 넣지 않는다.
