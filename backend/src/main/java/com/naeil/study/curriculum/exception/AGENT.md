# curriculum/exception — 에이전트 작업 규칙

## 지킬 것

- BusinessException 을 상속하고 ErrorCode 를 넘긴다.
- 진단용 값(reason)은 필드로 들고 있되 응답에 넣지 않는다.
- 제약 위반을 예외로 드러낸다. 잘못된 계획을 조용히 내보내지 않는다.

## 하지 말 것

- 예외 메시지에 내부 계산 값이나 세션 식별자 포함
- 계획을 만들 수 없는 상황을 500으로 응답
- 사용자가 다르게 행동할 수 없는 구분을 에러 코드로 나누기
