# analysis/exception — 에이전트 작업 규칙

## 지킬 것

- `BusinessException` 을 상속하고 `ErrorCode` 를 넘긴다.
- 400 계열 메시지에는 무엇을 하면 되는지 적는다.
- 진단용 값(`reason`)은 필드로 들고 있되 응답에 넣지 않는다.

## 하지 말 것

- 예외 메시지에 AI 응답 원문, 프롬프트, 강의자료 내용 포함
- 사용자가 다르게 행동할 수 없는 구분을 에러 코드로 나누기
- 분석 실패를 500으로 응답 (502가 원인을 더 정확히 가리킨다)
