# analysis/config — 에이전트 작업 규칙

## 지킬 것

- API 키를 코드에 두지 않는다. 환경변수로 받는다.
- 키가 없어도 애플리케이션이 뜨게 한다.
- 타임아웃을 반드시 설정한다.
- 모델 ID를 설정값으로 받는다.

## 하지 말 것

- API 키를 로그로 출력 (일부만 마스킹해서도 출력하지 않는다)
- 키가 없을 때 기동 실패시키기
- 여기에 분석 로직 넣기
- 테스트에서 실제 클라이언트를 등록 (Fake로 대체한다)
