# analysis/controller — 에이전트 작업 규칙

## 지킬 것

- 컨트롤러는 얇게 유지한다. AI 호출을 여기서 하지 않는다.
- Request Body를 받지 않는다. 필요한 데이터는 서버가 조회한다.
- 모든 경로를 `/api/sessions/{sessionCode}` 아래에 둔다.

## 하지 말 것

- 분석 진행률 API 추가
- 분석 상태 조회 API 추가 (세션 조회로 확인한다)
- 프론트에서 보낸 텍스트로 분석하기
- 실패 응답에 내부 원인(AI 오류 메시지) 노출
