# studycontext/repository — 에이전트 작업 규칙

## 지킬 것

- 조회는 항상 `studySessionId` 조건으로 한다.
- 세션당 하나이므로 `Optional` 을 돌려준다. 리스트를 돌려주지 않는다.
- 테스트에는 다른 세션의 데이터를 함께 넣어 격리를 확인한다.

## 하지 말 것

- 쓰지 않는 메서드 미리 추가
- 세션 범위를 벗어나는 전역 조회
- 내용(텍스트)을 조건으로 하는 검색 쿼리
