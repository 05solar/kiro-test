# storage/exception — 에이전트 작업 규칙

## 지킬 것

- 원인 예외는 로그에만 남긴다.
- 저장소 실패는 사용자 잘못이 아니므로 500으로 응답한다.
  단, 컨테이너가 잘라낸 대용량 업로드는 400(`FILE_SIZE_EXCEEDED`)이다. 혼동하지 않는다.

## 하지 말 것

- 예외 메시지에 파일 경로, root 경로, OS 메시지 포함
- IOException 을 그대로 컨트롤러까지 올리기
