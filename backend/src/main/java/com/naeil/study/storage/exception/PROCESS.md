# storage/exception 작업 절차

## 저장소 예외를 세분화할 때

지금은 하나로 충분하다. 나누기 전에 다음을 확인한다.

```
1. 사용자가 다르게 행동할 수 있는 구분인가?
   (없는 파일 / 권한 없음 / 디스크 가득 → 사용자는 모두 "다시 시도" 뿐이다)
2. 아니라면 로그로 구분하고 응답은 하나로 유지한다
```

## 검증

```bash
./gradlew test --tests "*LocalStorageServiceTest*"
```
