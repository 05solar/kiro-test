# topic/dto — 에이전트 작업 규칙

## 지킬 것

- `record` 로 만든다. 변환은 정적 팩터리 `from()` 에 둔다.
- 목록은 배열로 감싼 객체로 응답한다.
- `importance` 는 Enum 이름 그대로 내보낸다.

## 하지 말 것

- `sourceDocumentIds` 노출
- 화면 표기 문구("매우 중요")를 서버에서 만들기
- 엔티티를 그대로 반환
