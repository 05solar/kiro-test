# topic/controller — 에이전트 작업 규칙

## 지킬 것

- 컨트롤러는 얇게 유지한다. 매핑과 변환만 한다.
- 모든 경로를 `/api/sessions/{sessionCode}` 아래에 둔다.
- 빈 목록도 200으로 응답한다.

## 하지 말 것

- Topic 상세 조회 엔드포인트 추가
- 분석 실행 엔드포인트를 여기 두기 (analysis 도메인의 몫이다)
- 빈 목록에 404 응답
- 정렬 파라미터 추가 (기본 학습 순서를 유지한다)
