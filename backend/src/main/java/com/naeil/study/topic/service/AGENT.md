# topic/service — 에이전트 작업 규칙

## 지킬 것

- 생성자 주입만 쓴다.
- 세션은 `SessionService` 를 통해 가져온다.
- 빈 목록을 예외로 만들지 않는다. 분석 전 상태는 정상이다.

## 하지 말 것

- 이 서비스에 Topic 생성/수정/삭제 추가 (분석 도메인의 몫이다)
- AI 호출
- 남은 시간 기반 필터링이나 정렬 (커리큘럼 단계의 몫이다)
