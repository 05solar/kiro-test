# AI 에이전트 작업 규칙

이 저장소에서 코드를 생성하거나 수정하는 에이전트가 지켜야 할 규칙.

## 1. 단계를 넘지 않는다

이 프로젝트는 STEP 단위로 개발한다. 현재 단계에서 요청하지 않은 기능은 만들지 않는다.
"나중에 필요할 것 같아서" 미리 만드는 코드는 이 저장소에서 부채다.
단, **구조는** 다음 단계를 받아들일 수 있게 설계한다. (예: `remainingStudyMinutes` 필드는
지금 두되 차감 로직은 만들지 않는다.)

## 2. 각 작업 뒤에는 검증이 따른다

명령 하나를 수행하면 반드시 검증한다.

```
./gradlew test      # 기능 변경 후
./gradlew build     # 단계 완료 시
```

검증에 실패하면 다음 작업으로 넘어가지 않고 직전 작업을 다시 본다.
검증 실패와 조치는 `PROCESS.md`의 표에 남긴다.

## 3. 도메인 어휘를 지킨다

| 쓰는 말 | 쓰지 않는 말 | 이유 |
| --- | --- | --- |
| 학습 우선순위 | 시험 출제 가능성 | `importance`는 출제 확률이 아니라 학습 우선순위다 |
| 세션 코드 | 아이디, 사용자 번호 | 회원 개념이 없다 |
| StudySession | User | 최상위 도메인은 세션이다 |

## 4. 세션 코드는 접근 키다

`sessionCode`는 단순 식별자가 아니라 인증 수단이다. 따라서:

- 코드는 **서버에서만** `SecureRandom`으로 발급한다. 클라이언트가 지정할 수 없다.
- 존재하지 않는 세션과 만료된 세션은 **같은 메시지**로 응답한다. 코드 추측에 힌트를 주지 않는다.
- 내부 UUID(`id`)는 응답에 절대 포함하지 않는다.
- 에러 메시지에 내부 정보(생성 시각, 내부 ID, 스택트레이스)를 넣지 않는다.

## 5. 시간 값 두 개를 혼동하지 않는다

```
availableStudyMinutes  최초 입력한 전체 학습 가능 시간. 진행 중 변경 금지.
remainingStudyMinutes  현재 남아 있는 학습 가능 시간. STEP 완료마다 감소.
```

동적 커리큘럼 재조정은 `remainingStudyMinutes`를 기준으로 한다.
`availableStudyMinutes`를 깎는 코드를 작성하면 진행률과 재조정 기준이 함께 무너진다.

## 6. 시간은 `Clock` 빈으로 다룬다

`LocalDateTime.now()`를 직접 호출하지 않는다. 주입받은 `Clock`을 쓴다.
만료 정책이 이 서비스의 핵심 로직이라 테스트에서 시간을 고정할 수 있어야 한다.

같은 이유로 Bean Validation의 `@Future` / `@Past`를 쓰지 않는다. 시스템 시계를 보기 때문에
고정한 `Clock`과 결과가 어긋난다. 시각 비교는 서비스에서 하고 도메인 에러 코드로 응답한다.

### 시간 단위 정책 (프로젝트 전체 공통)

```
남은 시간 계산   ChronoUnit.MINUTES 기준, 초 단위는 버린다 (내림)
저장 타입        LocalDateTime (서버와 프론트가 같은 로컬 시간을 쓴다는 전제)
```

올려 잡아 계획이 시험 시각을 넘기는 것보다 내려 잡는 편이 안전하다.
타임존을 다루게 되는 시점이 오면 이 규칙을 한 곳에서 바꿀 수 있도록 계산은
`SessionService`의 계산 메서드에 모아 둔다.

## 7. 엔티티를 컨트롤러에서 반환하지 않는다

응답은 항상 DTO(record)로 변환한다. 엔티티 필드가 늘어나도 API 응답이 조용히 바뀌지 않게 한다.

## 8. 명세 모순을 발견하면 기록하고 진행한다

기획 문서에 서로 어긋나는 내용이 있으면, 더 구체적이고 여러 문서에서 일치하는 쪽을 채택하고
`PROCESS.md`에 판단 근거를 남긴 뒤 진행한다. 임의로 고르고 넘어가지 않는다.

현재까지 확인된 모순:

- **세션 코드 문자 집합**: 허용 문자열 `ABCDEFGHJKLMNPQRSTUVWXYZ23456789`에는 `L`이 있는데
  제외 목록에는 `L`이 있다. 문자열이 두 문서에서 동일하고 32자로 떨어지므로 문자열을 정본으로
  채택했다. (`1`과 `I`가 이미 빠져 있어 `L`은 혼동 대상이 없다.)
  `L`을 빼려면 `SessionCodePolicy.ALPHABET`과 `PATTERN` 두 상수만 고치면 된다.

## 9. 엔티티 필드를 추가하면 실제 PostgreSQL로 확인한다

테스트는 H2를 쓴다. H2에서 통과하는데 PostgreSQL에서 깨지는 매핑이 있다.

실제로 겪은 사례: `@Lob String` 을 쓰면 PostgreSQL JDBC 드라이버가 값을 large object로
저장하고 컬럼에는 OID 숫자만 남긴다. H2에서는 정상 동작해서 테스트 342개가 전부 통과했지만
DB에는 본문이 없었다. 긴 텍스트 컬럼은 `@Lob` 없이 `columnDefinition = "TEXT"` 만 쓴다.

새 필드를 추가한 단계에서는 Docker로 PostgreSQL을 띄워 저장 결과를 눈으로 확인한다.

## 10. 문서를 코드와 함께 고친다

API를 바꾸면 `docs/api/`의 해당 명세를 같은 커밋에서 고친다.
폴더를 추가하면 그 폴더에 `README.md` / `PROCESS.md` / `AGENT.md`를 함께 만든다.

## 11. 불필요한 리팩터링을 하지 않는다

기존 컨벤션이 있으면 그 컨벤션을 따른다. 취향 차이로 구조를 바꾸지 않는다.
